trigger:
  branches:
    include:
      - main
  paths:
    include:
      - src/Basket.API/*
      - src/EventBus/*
      - src/EventBusServiceBus/*
      - src/eShop.ServiceDefaults/*
      - tests/Basket.FunctionalTests/*
      - basket-api-azure-pipeline.yml
      - k8s/basket-api-deployment.yml
      - Directory.Packages.props
pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'
  acrName: 'acrkaseyaeshop'
  imageName: 'basket-api'
  aksResourceGroup: 'rg-kaseya'
  aksClusterName: 'kaseyaeShop-backend'

stages:
  - stage: Build
    jobs:
      - job: BuildAndPush
        steps:
          - task: DotNetCoreCLI@2
            displayName: 'Restore'
            inputs:
              command: restore
              projects: 'src/Basket.API/Basket.API.csproj'

         # - task: DotNetCoreCLI@2
          #  displayName: 'Test'
           # inputs:
            ## projects: 'tests/Basket.FunctionalTests/Basket.FunctionalTests.csproj'
           #   arguments: '--configuration $(buildConfiguration) --report-xunit-trx --report-xunit-trx-filename basket-tests.trx'
            #  publishTestResults: false
           # continueOnError: false

         # - task: PublishTestResults@2
          #  displayName: 'Publish test results'
           # condition: succeededOrFailed()
            #inputs:
             # testResultsFormat: 'VSTest'
              #testResultsFiles: '**/basket-tests.trx'
             # searchFolder: 'tests/Basket.FunctionalTests'

          - task: AzureCLI@2
            displayName: 'Build image from Dockerfile and push to ACR'
            inputs:
              azureSubscription: 'ado-to-azure-auth/authz'
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az acr build \
                  --registry $(acrName) \
                  --image $(imageName):$(Build.BuildId) \
                  --file src/Basket.API/Dockerfile \
                  .
# --file src/Basket.API/Dockerfile path for Dockerfile to buld by script.

  - stage: DeployToDev
    dependsOn: Build
    condition: succeeded()
    jobs:
      - deployment: DeployBasketApi
        environment: 'eshop-dev'
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self
                - task: AzureCLI@2
                  displayName: 'Deploy to AKS'
                  inputs:
                    azureSubscription: 'ado-to-azure-auth/authz'
                    scriptType: bash
                    scriptLocation: inlineScript
                    # kubelogin is not installed which is required to connect to AAD enabled cluster.
                    inlineScript: |
                      az aks install-cli 
                      az aks get-credentials --resource-group $(aksResourceGroup) --name $(aksClusterName) --overwrite-existing --admin
                      sed "s#$(acrName).azurecr.io/$(imageName):latest#$(acrName).azurecr.io/$(imageName):$(Build.BuildId)#" k8s/basket-api-deployment.yml | kubectl apply -f -
                      kubectl rollout status deployment/basket-api -n basket-api --timeout=120s
